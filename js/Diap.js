var intElemScrollTop = someElement.scrollTop;var intElemScrollTop = someElement.scrollTop;$(window).scroll(function (event) {
    var scroll = $(window).scrollTop();
    // Do something
});$(window).scroll(function (event) {
    var scroll = $(window).scrollTop();
    // Do something
});